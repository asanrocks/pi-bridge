// Placeholder: replaced at bundle time by the real base64-encoded web assets.
// The bundle script (scripts/bundle-single.mjs) overwrites dist/host/embedded-assets.js
// with the generated web-assets module before running esbuild.
//
// WARNING: npm run build alone produces this stub — the resulting CLI serves
// an empty page. Run npm run build:binary for a working distribution.
const embeddedAssets: Record<string, string> = {};
export default embeddedAssets;
